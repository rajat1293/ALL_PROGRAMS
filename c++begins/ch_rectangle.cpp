#include<iostream>
using namespace std;
class rectangle
{
	private:
	int a[4],b[4];
	public:
int set(int *c,int *d)
	{
		for(i=0;i<4;i++)
		{if(c[i]>=0&&d[i]>=0)
		{
		a[i]=c[i];
		b[i]=d[i];
		}
		else return 0;
		}
		return 1;
		if(a[0]-a[1])
		k=(b[0]-b[1])/(a[0]-a[1]);	
	}	
	
	
	
	
	
	
	
	
}
